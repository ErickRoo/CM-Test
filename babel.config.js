/**
 * babel.config.js
 *
 * Project-wide Babel configurations. Gatsby already ships with
 * it's own Babel configuration file(.babelrc), but having a project-wide
 * configuration file is useful for setting up other custom presets and
 * options not provided by Gatsby. The `babel-preset-gatsby` preset is
 * the default Babel configuration from Gatsby. For more information on
 * Gatsby + Babel, see: https://www.gatsbyjs.com/docs/how-to/custom-configuration/babel/
 *
 * Fore more information on Babel configuration options,
 * go to https://babeljs.io/docs/en/config-files#project-wide-configuration
 * and https://babeljs.io/docs/en/configuration
 */

module.exports = {
  // The `babel-preset-gatsby` preset is the default
  // Babel configuration from Gatsby.
  presets: ['babel-preset-gatsby'],
};

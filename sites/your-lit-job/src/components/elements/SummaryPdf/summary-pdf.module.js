const page = {
  backgroundColor: '#fff',
  position: 'relative',
};

const body = {
  marginTop: '-120px',
  height: '75%',
  display: 'flex',
  justifyContent: 'space-between',
};

const wrapperFooter = {
  width: '100%',
  display: 'flex',
  justifyContent: 'center',
  flexDirection: 'row',
};

const footer = {
  width: '60%',
  height: 'auto',
};

const summaryPdfModule = { page, body, wrapperFooter, footer };

export default summaryPdfModule;
